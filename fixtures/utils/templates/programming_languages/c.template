#include <stdio.h>

int main() {
    printf("Language is working and loops are running!\n");

    // Print numbers 1 to 5 using a for loop
    for (int i = 1; i <= 5; i++) {
        printf("Iteration: %d\n", i);
    }

    return 0;
}